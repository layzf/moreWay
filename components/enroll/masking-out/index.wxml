
<view class="commodity_screen" bindtap="hideModal" wx:if="{{showModalStatus}}" catchtouchmove="return"></view>

<view animation="{{animationData}}"
      style="height:{{heightMasking}}rpx"
      class="commodity_attr_box" 
      wx:if="{{showModalStatus}}" 
      catchtouchmove="return">
    <slot name="content"></slot>
</view>